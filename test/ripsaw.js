var expect = require('chai').expect
var ripsaw = require('../index')

describe('Ripsaw Events', function () {
  describe('events#on', function () {
    it('adds a channel with callback to the object', function () {
      
    })
  })
})
